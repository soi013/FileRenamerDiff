<UserControl
   x:Class="FileRenamerDiff.Views.SettingApp"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
   xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
   xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
   xmlns:v="clr-namespace:FileRenamerDiff.Views"
   xmlns:vm="clr-namespace:FileRenamerDiff.ViewModels"
   Height="Auto"
   d:DataContext="{d:DesignInstance {x:Type vm:SettingAppViewModel},
                                    IsDesignTimeCreatable=True}"
   d:DesignHeight="650"
   d:DesignWidth="800"
   mc:Ignorable="d">
   <UserControl.Resources>
      <Style x:Key="divider" TargetType="{x:Type Border}">
         <Setter Property="Height" Value="1" />
         <Setter Property="HorizontalAlignment" Value="Stretch" />
         <Setter Property="Background" Value="{StaticResource MaterialDesignDivider}" />
      </Style>
      <Style BasedOn="{StaticResource MaterialDesignOutlinedTextFieldTextBox}" TargetType="{x:Type TextBox}">
         <Setter Property="MinWidth" Value="40" />
         <Setter Property="materialDesign:HintAssist.IsFloating" Value="False" />
      </Style>
      <Style
         x:Key="AddButton"
         x:Shared="False"
         BasedOn="{StaticResource MaterialDesignRaisedDarkButton}"
         TargetType="{x:Type Button}">
         <Setter Property="Content">
            <Setter.Value>
               <StackPanel Orientation="Horizontal">
                  <materialDesign:PackIcon Kind="TableRowAddAfter" />
                  <TextBlock Margin="5,0" Text="ADD" />
               </StackPanel>
            </Setter.Value>
         </Setter>
         <Setter Property="Margin" Value="30,5" />
      </Style>
   </UserControl.Resources>
   <StackPanel>
      <Expander HorizontalAlignment="Stretch" Header="Ignore Extensions (File Load)">
         <Grid>
            <Grid.RowDefinitions>
               <RowDefinition Height="*" />
               <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DataGrid
               Grid.Row="0"
               AutoGenerateColumns="False"
               HeadersVisibility="All"
               ItemsSource="{Binding IgnoreExtensions}">
               <DataGrid.Columns>
                  <DataGridTextColumn
                     Binding="{Binding Value}"
                     EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                     Header="Ignore Extensions" />
               </DataGrid.Columns>
            </DataGrid>
            <Button
               Grid.Row="1"
               Command="{Binding AddIgnoreExtensionsCommand}"
               Style="{StaticResource AddButton}" />
         </Grid>
      </Expander>
      <Border Style="{StaticResource divider}" />
      <Expander HorizontalAlignment="Stretch" Header="DeleteTexts">
         <Grid>
            <Grid.RowDefinitions>
               <RowDefinition Height="*" />
               <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DataGrid
               AutoGenerateColumns="False"
               ColumnHeaderStyle="{StaticResource MaterialDesignDataGridColumnHeader}"
               HeadersVisibility="All"
               ItemsSource="{Binding DeleteTexts}">
               <DataGrid.Columns>
                  <DataGridTextColumn
                     Binding="{Binding TargetPattern}"
                     EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                     Header="Input" />
                  <DataGridCheckBoxColumn Binding="{Binding AsExpression}" Header="As&#xa;Expression" />
               </DataGrid.Columns>
            </DataGrid>
            <Button
               Grid.Row="1"
               Command="{Binding AddDeleteTextsCommand}"
               Style="{StaticResource AddButton}" />
         </Grid>
      </Expander>
      <Border Style="{StaticResource divider}" />
      <Expander HorizontalAlignment="Stretch" Header="Replace Patterns">
         <Grid>
            <Grid.RowDefinitions>
               <RowDefinition Height="*" />
               <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DataGrid
               AutoGenerateColumns="False"
               HeadersVisibility="All"
               ItemsSource="{Binding ReplaceTexts}">
               <DataGrid.Columns>
                  <DataGridTextColumn
                     Binding="{Binding TargetPattern}"
                     EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                     Header="Input" />
                  <DataGridTemplateColumn>
                     <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                           <materialDesign:PackIcon VerticalAlignment="Center" Kind="ArrowRight" />
                        </DataTemplate>
                     </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>
                  <DataGridTextColumn
                     Binding="{Binding ReplaceText}"
                     EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                     Header="Output" />
                  <DataGridCheckBoxColumn Binding="{Binding AsExpression}" Header="As&#xa;Expression" />
               </DataGrid.Columns>
            </DataGrid>
            <Button
               Grid.Row="1"
               Command="{Binding AddReplaceTextsCommand}"
               Style="{StaticResource AddButton}" />
         </Grid>
      </Expander>
      <Border Style="{StaticResource divider}" />
      <Expander HorizontalAlignment="Stretch" Header="Other">
         <StackPanel>
            <Button
               Margin="30,5"
               Command="{Binding ResetSettingCommand}"
               Style="{StaticResource MaterialDesignRaisedAccentButton}">
               <StackPanel Orientation="Horizontal">
                  <materialDesign:PackIcon Kind="Autorenew" />
                  <TextBlock Margin="5,0" Text="Reset ALL Setting" />
               </StackPanel>
            </Button>
         </StackPanel>
      </Expander>
   </StackPanel>
</UserControl>
