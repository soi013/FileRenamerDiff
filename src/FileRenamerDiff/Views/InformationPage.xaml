<UserControl
   x:Class="FileRenamerDiff.Views.InformationPage"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
   xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
   xmlns:local="clr-namespace:FileRenamerDiff.Views"
   xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
   xmlns:v="clr-namespace:FileRenamerDiff.Views"
   xmlns:vm="clr-namespace:FileRenamerDiff.ViewModels"
   d:DataContext="{d:DesignInstance {x:Type vm:InformationPageViewModel},
                                    IsDesignTimeCreatable=True}"
   d:DesignHeight="450"
   d:DesignWidth="800"
   Background="{StaticResource MaterialDesignPaper}"
   BorderBrush="{StaticResource PrimaryHueMidBrush}"
   BorderThickness="1"
   TextElement.Foreground="{StaticResource MaterialDesignBody}"
   mc:Ignorable="d">
   <StackPanel Margin="30,100" HorizontalAlignment="Center">
      <StackPanel Orientation="Horizontal">
         <Border Padding="5,1" CornerRadius="5" BorderThickness="2" Background="{DynamicResource BlackBackground}" BorderBrush="{DynamicResource AccentColorBrush}">
            <Image
               Width="180"
               Source="/Resources/FileRenamerDiff_icon.png"
               Stretch="Fill" />
         </Border>
         <RichTextBox
            Width="500"
            Margin="10"
            IsReadOnly="True">
            <FlowDocument>
               <Paragraph FontSize="18">
                  <Run
                     FontSize="48"
                     FontWeight="ExtraBold"
                     Text="File Renamer Diff" />
                  <LineBreak />
                  <LineBreak />
                  <Run Text="Made by " />
                  <Run FontStyle="Italic" Text="{Binding Author, Mode=OneTime}" />
                  <LineBreak />
                  <Run Text="Version " />
                  <Run FontStyle="Italic" Text="{Binding Version, Mode=OneTime}" />
                  <LineBreak />
                  <Run Text="Repository " />
                  <Run Foreground="{DynamicResource AccentColorBrush4}" Text="https://github.com/soi013/FileRenamerDiff" />
               </Paragraph>
            </FlowDocument>
         </RichTextBox>
      </StackPanel>
   </StackPanel>
</UserControl>
